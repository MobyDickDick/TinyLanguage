// Rosetta Code: recursive factorial ported from Python to TinyLanguage
// Run with: python src/tiny_language.py src_tiny/rosetta_factorial.tiny

fn fact(n) {
    if (n <= 1) { return 1; }
    return n * fact(n - 1);
}

define i = 1;
while (i <= 5) {
    print(i, fact(i));
    i = i + 1;
}
