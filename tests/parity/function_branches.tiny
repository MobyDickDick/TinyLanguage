// Parity: function branching should match across backends.
fn clamp(value, minimum, maximum) {
  if (value < minimum) {
    return minimum;
  }
  if (value > maximum) {
    return maximum;
  }
  return value;
}

print(clamp(-2, 0, 10));
print(clamp(5, 0, 10));
print(clamp(22, 0, 10));
