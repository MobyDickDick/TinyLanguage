fn fibonacci(count) {
    define a = 0;
    define b = 1;
    define i = 0;
    while (i < count) {
        print(a);
        define temp = a + b;
        a = b;
        b = temp;
        i = i + 1;
    }
}

fibonacci(10);
