// Gibt eine Zählvariable einige Male aus, damit der Runner nicht hängen bleibt.
define max_iterations = 10;
define i = 0;
while (i < max_iterations) {
  print("This is a simple Test");
  print(i);
  i = i + 1;
}
