// Rosetta example calculating a factorial iteratively and printing the result.

fn factorial(n) {
    define result = 1;
    define i = 2;
    while (i <= n) {
        result = result * i;
        i = i + 1;
    }
    return result;
}

define _ = print(factorial(5));
