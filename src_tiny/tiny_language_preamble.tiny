// TinyLanguage wrapper for src/tiny_language_preamble.py.
// Exposes shared constants and error formatting helpers via Python interop.

def __PY_ALLOW = new["BUILTINS", "KEYWORDS", "TinyLangError", "format_error"];

def __PY = Python.import_module("tiny_language_preamble", __PY_ALLOW);

def BUILTINS = __PY.BUILTINS;
def KEYWORDS = __PY.KEYWORDS;
def TinyLangError = __PY.TinyLangError;
def format_error = __PY.format_error;

def _exports = new[BUILTINS, KEYWORDS, TinyLangError, format_error];
