// TinyLanguage wrapper for src/run_all.py.
// Runs the full TinyLanguage demo/test suite via the Python script.

def PY_ALLOW = new["main"];

def PY = Python.import_module("run_all", PY_ALLOW);

def main = PY.main;

fn run() {
    return main();
}

if (run() == Null) { }
