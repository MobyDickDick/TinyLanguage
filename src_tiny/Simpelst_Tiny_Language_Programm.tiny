// Prints a counter variable a few times so the runner does not hang.
define max_iterations = 10;
define i = 0;
while (i < max_iterations) {
  print("This is a simple Test");
  print(i);
  i = i + 1;
}
