module: .vscode.all_features

exports:

types:
  type Error = { code: number, msg: string }

classes:
  class Counter
    field label: string
    field value: number
  class FancyCounter
    field bonus: number
