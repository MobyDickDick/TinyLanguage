// TinyLanguage wrapper for src/formatter.py.
// Delegates formatting helpers to the Python implementation.

def __PY_ALLOW = new["format_source", "format_import"];

def __PY = Python.import_module("formatter", __PY_ALLOW);

def format_source = __PY.format_source;
def format_import = __PY.format_import;

def _exports = new[format_source, format_import];
