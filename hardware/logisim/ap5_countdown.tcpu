; AP 5 core-only fixture: prints 3, 2, 1 and halts normally.
decrement := 101
counter := 100

LOAD_CONST(-1)
STORE_ADDRESS(decrement)
LOAD_CONST(3)
STORE_ADDRESS(counter)
loop:
PRINT()
ADD_CONST(-1)
STORE_ADDRESS(counter)
JUMP_NOT_ZERO(loop)
HALT()
